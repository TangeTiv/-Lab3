这个实验的任务量相对较大，不仅需要修改底层编译器的核心代码（词法、语法分析），还需要使用 C++ 编写一个带图形界面的 Windows 应用程序。

为了高效完成，我建议将整个实验任务拆分为以下 **六个阶段**。你可以按照这个顺序逐步攻克：

### **阶段一：前置准备与 GUI 框架选型**

在动手改编译器代码之前，需要先搭好 C++ 的开发环境和界面基础。

* **1\. 熟悉原有代码结构：** 重点阅读 GLOBALS.H（语法树结构）、SCAN.C（词法扫描流程）、PARSE.C（递归下降语法分析）和 UTIL.C（语法树打印）。  
* **2\. 确定 C++ GUI 框架：** 实验要求应用程序必须是 Windows 窗口式图形界面，且实现语言只能用 C++。你可以选择 **MFC**（经典，与示例图风格最像）、**Qt**（跨平台，现代化，推荐）或 **C++ Builder / ImGui**。  
* **3\. 搭建基础界面：** 建立一个包含“代码编辑区（左侧）”和“语法树显示区（右侧）”的基础窗口。

### **阶段二：词法分析器扩充（底层修改 第一步）**

这一步的目标是让扫描器（Scanner）能认出你新加的符号和关键字。

* **1\. 修改 GLOBALS.H：** 在 TokenType 枚举中增加新的单词标记（Token）。  
  * **关键字：** WHILE, ENDWHILE, FOR。  
  * **运算符：** INC (++), DEC (--), MOD (%), POWER (^), LE (\<=), GT (\>), GE (\>=), NEQ (\<\>), OR (|), AND (&), CLOSURE (\#), OPTIONAL (?) 等。  
  * **特殊赋值号：** REGEX\_ASSIGN (::=)。  
* **2\. 修改 SCAN.C：**  
  * 在 reservedWords 数组中添加 while, endwhile, for。  
  * 修改 getToken() 函数中的状态机（DFA），使其能够正确识别双字符运算符（比如遇到 \< 时，要向后看一位，如果是 \= 则返回 LE，如果是 \> 则返回 NEQ；遇到 : 也要向后看，判断是 := 还是 ::= 等）。

### **阶段三：语法分析器与语法树扩充（底层修改 第二步）**

这是最核心的一步，需要设计文法并修改解析器（Parser）生成新的抽象语法树（AST）。

* **1\. 扩充语法树节点 (GLOBALS.H)：** \* 在 StmtKind 中增加如 WhileK, ForK, RegexAssignK。  
  * 在 ExpKind 中增加正则表达式节点类型等。  
* **2\. 设计扩展后的 EBNF 文法：** 根据实验要求，写出新语句和表达式的产生式（这需要你自己组织）。例如：  
  * while-stmt \-\> while (exp) stmt-sequence endwhile  
  * for-stmt \-\> for (assign-stmt ; exp ; assign-stmt) stmt-sequence  
* **3\. 编写递归下降子程序 (PARSE.C)：**  
  * 新增解析函数：编写 while\_stmt(), for\_stmt(), regex\_exp() 等函数。  
  * 修改原有函数：修改 statement() 分支以接入 while 和 for；修改 exp()、term()、factor() 等函数，把 \++, \--, %, ^ 以及新增的关系运算符加入到解析逻辑中。

### **阶段四：界面功能实现与前后端对接**

将修改好的底层编译器代码与你第一阶段写好的 GUI 界面结合起来。

* **1\. 实现文件 I/O：** 在界面上实现“打开源程序”、“保存文件”的功能。  
* **2\. 重定向输入输出：** 原版 TINY 是从命令行读取文件路径并输出到终端的 stdout。你需要将界面左侧文本框中的代码传入给你的 parse() 函数；并将 printTree() 生成的文本输出重定向，捕获为字符串。  
* **3\. 渲染语法树：** 将捕获到的语法树字符串结构，解析并绑定到 GUI 框架的树形控件（如 MFC 的 CTreeCtrl 或 Qt 的 QTreeView）中，实现如示例图所示的可折叠树状结构。

### **阶段五：测试用例编写与调试**

* **1\. 改写基础测试用例：** 按照实验指导书，将提供的测试文件1和2按照你设计的文法格式（如增加括号等）进行改写。  
* **2\. 全面覆盖测试：** 自己编写包含正则表达式（|, &, \#, ?）、::= 赋值、复合四则运算的综合测试文件。  
* **3\. 错误恢复测试：** 故意写错一些语法，测试程序能否像示例图中那样，在下方的消息框中输出行号和 Syntax Error 提示。

### **阶段六：文档编写（选做，但建议做）**

* 整理你的文法规则（EBNF）。  
* 画出主要扩展语法（如 for 循环和 while 循环）的语法树结构设计图。  
* 编写软件使用说明文档。

**下一步建议：** 你可以先规划一下准备用哪种 C++ 框架来做界面（MFC 还是 Qt？），或者我们直接从 **阶段二：词法扫描器的扩充** 开始讨论如何修改代码？